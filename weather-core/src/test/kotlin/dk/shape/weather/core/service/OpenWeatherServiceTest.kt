package dk.shape.weather.core.service

import io.mockk.every
import io.mockk.mockk
import dk.shape.weather.regulator.cache.WeatherCacheService
import org.junit.jupiter.api.Test

class OpenWeatherServiceTest {

//    val expectedForecast
    private val cacheService = mockk<WeatherCacheService> {

}

    @Test
    fun `test getForecastForLocation service`() {
        assert(false)
    }
}